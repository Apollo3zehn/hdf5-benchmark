# Do not run this workflow on pull request since this workflow has permission to modify contents.
on:
  push:
    branches:
      - master

permissions:
  # deployments permission to deploy GitHub pages website
  deployments: write
  # contents permission to update benchmark contents in gh-pages branch
  contents: write

jobs:
  benchmark:
    name: Run benchmarks
    runs-on: ubuntu-latest
    steps:
      
      - uses: actions/checkout@v4
        with:
          submodules: "true"

      - name: Generate test files
        run: |
          python --version
          cd data
          python -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt

          python data/chunked_btree1_no_filter_1d.py
          python data/chunked_btree1_no_filter_2d.py

      - name: Run baseline benchmarks
        run: |
          dotnet --info
          cd baseline
          dotnet run -c Release --job short --filter '*' --project src/Benchmarks/Benchmarks.csproj

      - name: Run dotnet-purehdf benchmarks
        run: |
          dotnet --info
          cd dotnet-purehdf
          dotnet run -c Release --job short --filter '*' --project src/Benchmarks/Benchmarks.csproj

      - name: Run java-jhdf benchmarks
        run: |
          java -version
          cd java-jhdf/test
          mvn --batch-mode clean verify
          java -jar target/benchmarks.jar -rf json

      - name: Run python-pyfive benchmarks
        run: |
          python --version
          cd python-pyfive
          python -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt
          PYTHONPATH=external/pyfive pytest benchmarks/ --benchmark-json output.json
        
      # # gh-pages branch is updated and pushed automatically with extracted benchmark data
      # - name: Store benchmark result
      #   uses: benchmark-action/github-action-benchmark@v1
      #   with:
      #     name: chunked_btree1_no_filter_1d
      #     tool: 'benchmarkdotnet'
      #     output-file-path: dotnet-purehdf/BenchmarkDotNet.Artifacts/results/Benchmarks.chunked_btree1_no_filter_1d-report-full.json
      #     # Access token to deploy GitHub Pages branch
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     # Push and deploy GitHub pages branch automatically
      #     auto-push: true